name: Build, Test, Integration Test

on: [push]

jobs:
  test-build-integration-test:
    runs-on: ubuntu-latest
    env:
      TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
    strategy:
      matrix:
        node: [20, 22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - uses: pnpm/action-setup@v2
        name: Install pnpm
        id: pnpm-install
        with:
          version: 9
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install

      - name: Install xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Setup Firefox
        id: setup-firefox
        uses: browser-actions/setup-firefox@v1

      - name: Install Playwright
        run: pnpm --filter integration-test exec playwright install --with-deps chromium webkit

      - name: Build Firefox Extension
        run: pnpm --filter tab-manager-v2 build:firefox

      - name: Zip Firefox Extension
        run: pnpm --filter tab-manager-v2 zip:firefox

      - name: Firefox Target Tests
        run: pnpm --filter tab-manager-v2 test:firefox

      - name: Firefox Integration Test
        env:
          FIREFOX_E2E: '1'
        run: pnpm --filter integration-test-firefox test:firefox

      # Run all test
      - name: Integration Test
        run: xvfb-run --auto-servernum --server-args="-screen 0 1280x960x24" pnpm run test --force

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
